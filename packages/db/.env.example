# Copy this file to .env and fill in the values.
# Needed for commands run directly inside packages/db
# (prisma migrate, prisma studio, seed). The value must be the same
# as DATABASE_URL in apps/web/.env.local.

DATABASE_URL="postgresql://admin:password123@localhost:5433/family_db"
